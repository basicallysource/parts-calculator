node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Slicer scratch (3mf, cache, resolved profiles) — regenerated locally
slicer/build/
__pycache__/

# Note: src/lib/data/parts.generated.json, static/renders/, and static/stl/
# are generated by slicer/filament.py but ARE committed — Vercel serves them.

# stray OrcaSlicer logs
slicer/*.log

# raw STL drop zone (sort into slicer/parts/ before committing)
slicer/inbox/
