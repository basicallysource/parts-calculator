# 3D model binaries tracked with Git LFS to keep history lean.
# Vercel smudges LFS files on checkout, so static/stl/*.stl still deploy.
*.stl filter=lfs diff=lfs merge=lfs -text
*.zip filter=lfs diff=lfs merge=lfs -text
